// import React from 'react'
// import { render } from '@testing-library-react'
import createBlorp from './functions'

describe('createBlorpz unit tests', () => {
    test('createBlorp creates blorp that is alive', () => {
        expect(createBlorp.alive).toBe(true)
    })
    
    // test('createBlorp creates blorp that is awake', () => {
    
    // })
    
    // test('createBlorp creates blorp with happy level greater than 0', () => {
    
    // })
    
    // test('createBlorp creates blorp with a happy level less than 10', () => {
    
    // })
    
    // test('createBlorp creates blorp with a Hunger level greater than 0', () => {
    
    // })
    
    // test('createBlorp creates blorp with a hunger level less than 10', () => {
        
    // })
    
    // test('createBlorp creates blorp with one poop', () => {
    
    // })
    
    // it('Adds a blorp to the blorpz array', () => {
    
    // })
})
// createBlorp




// getBlorp

// updateBlorp


